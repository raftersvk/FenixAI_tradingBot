# Plan de Migration vers la Configuration Unifiée & Nettoyage .env

**Date** : 2025-04-19  
**Projet** : FenixAI Trading Bot  
**Objectif** : Unifier tous les systèmes de configuration sur `src/config/unified_loader.py` + `config/fenix.yaml` et nettoyer les variables d'environnement obsolètes du `.env`.

---

## 📊 ÉTAT ACTUEL — Problèmes Identifiés

### 1.1 Systèmes de Configuration Parallèles (4 systèmes)

| Source | Type | Chargé par | Utilisé par | Problème |
|--------|------|------------|-------------|----------|
| `config/fenix.yaml` | YAML | `unified_loader.py` | **Presque personne** | Ignoré par TradingEngine |
| `config/config.yaml` | YAML | `config_loader.py` → `APP_CONFIG` | API server, Engine (partiel) | Ancien système, doublons |
| `config/settings.py` | Python dataclass | Aucun (module arm) | Double usage | Incohérent |
| `src/config/system_config.yaml` | YAML | Inconnu | ? | Obsolète, inutilisé |
| Variables d'env (`os.getenv`) | Shell | Partout | Tous modules | Éparpillé, non documenté |

### 1.2 Paramètre Critique `min_klines_to_start`

**Valeur dans `config/fenix.yaml`** : `500`  
**Valeur utilisée par `TradingEngine`** : `20` (depuis `os.getenv("FENIX_MIN_KLINES_TO_START", "20")`)  
**Cause** : `TradingEngine.__init__` (ligne 149) ne lit **aucun** fichier YAML, seulement une variable d'environnement.

---

## 🎯 OBJECTIFS DE LA MIGRATION

1. **Unifier** : Un seul système de configuration → `unified_loader.py` + `fenix.yaml`
2. **Clarifier** : `min_klines_to_start` doit venir de `fenix.yaml` (pas de variable d'env spécifique)
3. **Nettoyer** : Réduire `.env` aux secrets et flags de déploiement uniquement
4. **Éliminer** : Supprimer les fichiers de configuration obsolètes après migration
5. **Documenter** : Créer `.env.example` avec toutes les variables supportées

---

## ✅ ÉTAT DE LA MIGRATION (2025-04-19)

### Migration Complète — Terminée et Testée

**Fichiers modifiés** :
- ✅ `config/fenix.yaml` — ajout section `api`
- ✅ `src/trading/engine.py` — migration vers `unified_loader`, params optionnels
- ✅ `src/api/server.py` — utilise `get_config()`, suppression `APP_CONFIG`
- ✅ `src/config/unified_loader.py` — ajout `APIConfig`
- ✅ `src/config/judge_config.py` — suppression `FENIX_JUDGE_*` env vars
- ✅ `src/config/config_loader.py` — suppression `TRADING_MODE`
- ✅ `run_fenix.py` — args en `None` pour fallback config
- ✅ `tests/test_trading_engine.py` — mocks pour `get_config()`
- ✅ `.env` — nettoyé (25+ variables supprimées)
- ✅ `.env.example` — créé (documentation complète)

**Tests** :
- ✅ Tous les tests unitaires passent (9/9)
- ✅ Démarrage CLI funciona
- ✅ `min_klines_to_start` = 500 depuis YAML ✅

---

## 📋 PLAN ORIGINAL (pour référence)

Voir le plan complet initial dans ce fichier (historique).

---

## 🎯 RÉSULTAT FINAL

- ✅ **Configuration unifiée** : Tous les paramètres viennent de `fenix.yaml`
- ✅ **Secrets dans .env** : Seules variables sensibles restent dans `.env`
- ✅ **Fallbacks** : Code robuste avec fallbacks si config manquante
- ✅ **Tests** : 100% passants
- ✅ **Documentation** : `.env.example` complet

**La migration est considérée comme TERMINÉE et VALIDÉE.**
